import { Link } from "react-router-dom";
import { useSelector } from "react-redux";
import "./navbar.css"
const Navbar = ({ click }) => {

  return (
   <h1>navbar</h1>
  );
};

export default Navbar;